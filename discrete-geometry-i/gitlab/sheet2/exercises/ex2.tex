%2
Recall the function space $\mathcal{C}(\R^{d})$ and show that the (ring) multiplication of the underlying $\R$-algebra structure is well-defined. Is $\mathcal{C}(\R^{d})$ a finite dimensional real vector space?
